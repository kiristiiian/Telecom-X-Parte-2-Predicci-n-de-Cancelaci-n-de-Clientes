# Telecom-X-Parte-2-Predicci-n-de-Cancelaci-n-de-Clientes
Carga y Limpieza de Datos En la descripción pon: "Se carga el dataset de Telecom X previamente procesado en la Fase 1. Se eliminan columnas de identificación (como customerID) que no aportan patrones predictivos y podrían causar sobreajuste (overfitting)."  Acción: Usa df.drop(columns=['customerID'], inplace=True).
